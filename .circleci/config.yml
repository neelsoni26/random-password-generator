version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.10.6
    steps:
      - checkout
      - run: python3 password_generator.py
  test:
    docker:
      - image: cimg/python:3.10.6
    steps:
      - checkout
      - run: python3 test_password_generator.py

workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
